clone:
  git:
    image: plugins/git
    depth: 50
    tags: true
pipeline:
  docker:
    image: plugins/docker
    secrets: [ docker_hub_username, docker_hub_password ]
    repo: hwan9/drone-ftps
    tags: latest